apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-backend-report
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "sausage.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector: {app.kubernetes.io/instance: {{ .Release.Name }}, app.kubernetes.io/component: backend-report}
  ports: [{name: http, port: 8080, targetPort: http}]
